spring.application.name=amani-apirest
server.port=8080
spring.datasource.url=jdbc:postgresql://localhost:5432/psicologia_app
spring.datasource.username=postgres
spring.datasource.password=Sandia4you
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- Spring Mail -----------------------------------------------------------
# Cambiar host/port/username/password por los valores del servidor SMTP real.
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=noreply@amanipsicologia.com
spring.mail.password=CAMBIAR_POR_APP_PASSWORD
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# --- Migracion BD (fcm_token) -------------------------------------------
# Ejecutar antes de arrancar si la columna no existe en la tabla usuarios:
# ALTER TABLE usuarios ADD COLUMN fcm_token VARCHAR(512);
