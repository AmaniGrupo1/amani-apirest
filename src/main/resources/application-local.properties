# =============================================================================
# Perfil LOCAL — Configurado para usar FIREBASE REAL en la nube
# =============================================================================

# --- No importar secretos en local ---
spring.config.import=optional:sm://

# --- Datasource local ---
spring.datasource.url=jdbc:postgresql://localhost:5433/postgres
spring.datasource.username=postgres
spring.datasource.password=Sandia4you
spring.jpa.show-sql=true

# --- Firebase (HABILITADO REAL) ---
firebase.enabled=true
firebase.emulator.enabled=false
firebase.database.url=https://amani-160bf-default-rtdb.europe-west1.firebasedatabase.app/
firebase.credentials-path=src/main/resources/firebase/amani-service-account.json

# --- Logging local ---
logging.level.com.amani.amaniapirest=DEBUG
logging.level.com.amani.amaniapirest.gateway.noop=INFO
logging.level.com.google.firebase=DEBUG
